# v0.4 hypothesis-loop acceptance — 2026-08-29

## Result

The v0.4 exit gate passed on the repository `main` branch. The implementation
acceptance boundary is `3ac61e1` (`test(acceptance): prove v0.4 hypothesis loop`),
with deterministic point-in-time read models in `0b97702`. The complete acceptance
is available through `make research-acceptance` and uses an isolated temporary
PostgreSQL database, the real `invs-research` collector image, and a local Jupyter
fixture. It does not require provider credentials or network evidence.

## Scenario covered

The acceptance drill seeds the reviewed AI-infrastructure theme and verifies the
following decision-date graph at `2026-08-29T12:00:00Z`:

| Object | Count |
| --- | ---: |
| Reviewed theme nodes | 7 |
| Entities | 8 |
| Theme memberships | 7 |
| Relationship revisions | 6 |
| Indicators | 2 |
| Feature references | 2 |
| Invalidation conditions | 3 |

The same cutoff produces a deterministic theme snapshot. Database-generated
operational creation timestamps are excluded from the snapshot read model, while
reviewed revision timestamps, validity intervals, evidence references, and record
hashes remain part of the output.

The local document fixture then proves the raw-first research path:

- one retained document manifest is published and linked to an entity;
- the raw artifact and deterministic text artifact are registered separately;
- the event proposal carries a source character span and source-document hash;
- one intentionally incorrect proposal is rejected; and
- one reviewed theme-exposure proposal is accepted.

The evidence pack contains only the theme snapshot, canonical observations, feature
artifact, retained document, and accepted event that were available at the decision
cutoff. A later document reference is rejected. Markdown/JSON memo export and
re-import preserve the memo identity and referenced artifact IDs.

The hypothesis ledger records a dated thesis with explicit causal model, benchmark,
bounded universe, review date, and invalidation conditions. The prediction is frozen
before measurement. An attempted direct update is rejected by the database, while
the measurement is later computed from the pinned price-artifact hash under
`close_to_close_equal_weight_v1` and registered as a measured outcome.

## Fail-closed probes

The isolated database acceptance verifies that these mutations are rejected:

1. updating a frozen prediction;
2. inserting an accepted event revision without reviewer authorization;
3. inserting a relationship revision with an empty validity interval;
4. skipping a theme revision number; and
5. updating an append-only theme revision in place.

The Python fixture additionally verifies source-span hashes, document-derived
artifact hashes, future-reference rejection, measurement input hashes, and memo
round-trip identity. All event revision states are retained as
`proposed,rejected,proposed,accepted`.

## Reproduction commands

The acceptance path is:

```sh
make research-acceptance
```

It replays every migration into a temporary database, seeds the theme through the
collector image, drives the metadata CLI, and retains generated evidence under
`data/research/acceptance/v0.4`. The final acceptance artifact summary records:

- theme `10000000-0000-4000-8000-000000000001`;
- evidence pack `b93600db-7ee8-52f6-89d2-d0b30746f649`;
- memo `46e15487-26df-5e65-96f4-a8df3de6f3a1`;
- hypothesis `72000000-0000-4000-8000-000000000001`;
- prediction `73000000-0000-4000-8000-000000000001`; and
- measured outcome `b6e44df3-b530-5d96-8d69-34056801f3d0`.

## Full repository validation

The final repository ladder passed on 2026-08-29:

- `make test` — Go tests and vet, 41 JSON Schema documents, 140 Python tests,
  and Ruff;
- `make research-acceptance` — isolated v0.4 database and end-to-end research
  workspace acceptance;
- `make historical-truth-db-test` — migration replay, append-only constraints,
  transaction rollback, and fresh-image wiring;
- `make notebook` — executed the vertical-slice notebook in a disposable Jupyter
  process;
- `make dashboard-smoke` — dashboard SQL validation and PostgreSQL smoke checks;
- `make migrate` — idempotent PostgreSQL migration checks;
- `make reconcile` — `issues=0`; and
- `make health` — PostgreSQL, Jupyter, and Grafana healthy.

The validation commands leave no tracked repository changes. Generated acceptance
artifacts are ignored runtime evidence and can be regenerated by the acceptance
command.

## Implementation checkpoints

- `4752a5d` — v0.4 ADR, migrations, and strict research schemas;
- `8b9e4e7` — append-only PostgreSQL research repository;
- `a486240` — policy-pinned prediction measurement;
- `897e7d8` — metadata workflow CLI;
- `cb64fd5` — reviewed event proposals;
- `17cbbc2` — point-in-time evidence packs and memo round trip;
- `a84a2b2` — theme indicators, feature references, and conditions;
- `161a23e` — reviewed AI-infrastructure reference bundle;
- `b6d6bd3` — point-in-time theme snapshot and status report;
- `cb93e52` — evidence and measurement contract hardening;
- `0b97702` — deterministic snapshot read model; and
- `3ac61e1` — isolated acceptance drill and operator target.

This closes v0.4 theme intelligence and the hypothesis research loop. Strategies,
backtesting, portfolio construction, paper trading, and live execution remain later
roadmap boundaries.
